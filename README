MySQL tests
===========    
    - MySQL tables must not exists before sending the test
    - The tables are created and included in the time
    
MongoDB database creation
=========================
> use InternetAccessLog;
> db.addUser("mongodb", "mongodb");


MySQL database creation
=======================
mysql> create database InternetAccessLog;
mysql> grant all privileges on InternetAccessLog.* to mysqldb@localhost identified by 'mysqldb';
